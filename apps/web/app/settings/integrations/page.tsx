'use client';
import { CheckCircle2, ExternalLink, LoaderCircle, LockKeyhole, MessageSquareText, PlugZap, ShieldCheck, TicketCheck } from 'lucide-react';
import { useEffect,useState } from 'react';
import { AppShell } from '@/components/app-shell';
import { api } from '@/lib/api';
type Integration={provider:string;status:string;mode:string;display_name:string};
export default function Integrations(){const [items,setItems]=useState<Integration[]>([]);const [testing,setTesting]=useState('');const [message,setMessage]=useState('');useEffect(()=>{api<Integration[]>('/integrations').then(setItems).catch(()=>location.href='/login')},[]);async function test(provider:string){setTesting(provider);setMessage('');try{const result=await api<{status:string}>(`/integrations/${provider}/test`,{method:'POST'});setMessage(`${provider} ${result.status} — no external write was performed.`)}catch(err){setMessage(err instanceof Error?err.message:'Test failed')}finally{setTesting('')}}return <AppShell title="Slack & Jira integrations" eyebrow="ADMINISTRATIVE CONNECTIONS"><section className="integration-intro"><div><ShieldCheck/><span><h2>Preview first. Approve every write.</h2><p>Credentials remain server-side and are never exposed to the browser, agents, or log evidence.</p></span></div><em><LockKeyhole/>Mock sandbox mode</em></section><section className="integration-grid">{items.map(item=><article key={item.provider}><header>{item.provider==='slack'?<MessageSquareText/>:<TicketCheck/>}<span><b>{item.provider==='slack'?'Slack':'Jira Cloud'}</b><small>{item.display_name}</small></span><em><i/>{item.status}</em></header><div><span>ADAPTER MODE</span><b>{item.mode}</b></div><div><span>EXTERNAL WRITES</span><b>Human approval required</b></div><div><span>{item.provider==='slack'?'SCOPE':'SCOPES'}</span><b>{item.provider==='slack'?'chat:write':'read:jira-work · write:jira-work'}</b></div><footer><button onClick={()=>test(item.provider)} disabled={!!testing}>{testing===item.provider?<LoaderCircle className="spin"/>:<PlugZap/>}Test connection</button><a href="/architecture">Security model <ExternalLink/></a></footer></article>)}</section>{message&&<div className="integration-message"><CheckCircle2/>{message}</div>}<section className="mock-note"><span className="kicker">OFFICIAL OAUTH ADAPTERS</span><h2>Ready when the workspace is.</h2><p>The integration boundary and scopes are documented, but real account connection remains intentionally disabled in this local implementation. Configure the server-side OAuth secret store and enable official adapters only in a dedicated Slack workspace and Jira sandbox.</p></section></AppShell>}
