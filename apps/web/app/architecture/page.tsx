import { ArrowLeft, CheckCircle2, Database, GitBranch, LockKeyhole, Network, Server, ShieldCheck } from 'lucide-react';

const contracts=[['Validate & normalize','Validated file manifests','Structured events with exact-line provenance'],['Classify & correlate','Incident-scoped event references','Evidence-backed findings and correlation groups'],['Recommend','Findings and cited evidence','Containment, diagnosis, remediation, validation, and rollback'],['Human review','Immutable external-action drafts','Approved, rejected, or edited decision record'],['Deliver & synthesize','Approved payloads and recommendations','Mock delivery references and Markdown cookbook']];
export default function Architecture(){return <main className="architecture-page"><header><a href="/"><ArrowLeft/>Product</a><span className="brand"><span className="brand-mark"><Network/></span>DevOps <em>IAS</em></span><a className="app-cta" href="/signup">Open workspace</a></header><section className="architecture-hero"><span className="kicker">SYSTEM ARCHITECTURE // VERSION 0.1</span><h1>Evidence flows forward.<br/><em>Authority does not.</em></h1><p>Every component is designed around one invariant: agents may analyze and recommend, but only an authorized human can release an external action.</p></section><section className="topology"><article><Server/><b>React web app</b><span>REST + resumable SSE</span></article><i>→</i><article><ShieldCheck/><b>FastAPI service</b><span>Auth, policy, validation</span></article><i>→</i><article><GitBranch/><b>LangGraph worker</b><span>Typed incident state</span></article><i>→</i><article><Database/><b>SQLite + FTS5</b><span>History and provenance</span></article></section><section className="architecture-grid"><article><span className="kicker">TRUST BOUNDARIES</span><h2>Safety is structural</h2>{['Secrets remain server-side','Log text is untrusted evidence','Workspace authorization is server-enforced','Action approval binds to a payload hash','Remediation commands are never executed'].map(x=><p key={x}><CheckCircle2/>{x}</p>)}</article><article><span className="kicker">RUNTIME</span><h2>Local-first, production-shaped</h2><p>The MVP runs with Docker Compose, SQLite, protected file storage, Redis, and one Python worker. Production interfaces map to PostgreSQL, managed Redis, object storage, and horizontally scaled workers.</p><div className="architecture-note"><LockKeyhole/><span><b>Mock by default</b>Slack and Jira adapters produce deterministic sandbox references until an Admin configures official OAuth credentials.</span></div></article></section><section className="contract-table"><span className="kicker">AGENT CONTRACTS</span><h2>One typed incident state</h2>{contracts.map(row=><div key={row[0]}>{row.map(cell=><span key={cell}>{cell}</span>)}</div>)}</section></main>}
